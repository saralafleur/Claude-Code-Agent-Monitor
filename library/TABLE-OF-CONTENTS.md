# Library — Table of Contents

> The single referential index of all durable knowledge. **Load this once**, then
> read only the specific entries whose **description** matches your task. Do not
> re-read it if it is already in your context.
>
> This file is generated by the `librarian-archivist` from each entry's
> frontmatter. Do not hand-edit rows; edit the entry and let the archivist
> regenerate. Last regenerated: 2026-07-29.

## How to use
1. Scan the **Description** column for entries whose trigger words match what you
   are about to do.
2. Open only those entries (the **Path** column).
3. If nothing matches but you *expected* it to, note it — that is a description
   miss the `/librarian audit` should fix.

---

## architecture

### dev-environment
| Topic | Description (when to load) | Path | Updated |
|-------|----------------------------|------|---------|
| Dev-stack port topology — dev backend, Vite client, and a possible stray prod build all compete for the same port number | Load when a dashboard browser tab isn't reflecting edits to `client/src`, when diagnosing "hot reload doesn't seem to work", when something is already listening on port 4820 (or whatever `DASHBOARD_PORT` resolves to) and it's unclear what, or when using the `devops` skill's `/devops web-up` (PID file `~/.claude/.ccam-web-dev.pid`, log `~/.claude/.ccam-web-dev.log`). Explains why the same port number can be either the real dev API (no UI at all) or a stale `client/dist/` production build, and gives concrete ways to tell them apart before assuming either one. | `knowledge/architecture/dev-environment/dashboard-dev-stack-topology.md` | 2026-07-29 |

## frontend

### conventions
| Topic | Description (when to load) | Path | Updated |
|-------|----------------------------|------|---------|
| Pattern — injecting build-time constants via Vite's `define` (`__APP_VERSION__`, `__DASHBOARD_PORT__`) | Load when you need a value baked into the client bundle at build time (version strings, a backend origin/port, a fixed feature flag) rather than fetched or configured at runtime — covers the three places such a global must be touched in this repo (`client/vite.config.ts`'s `define`, mirrored in `client/vitest.config.ts` for tests, declared in `client/src/vite-env.d.ts`) and the existing precedent (`__APP_VERSION__`, `__DASHBOARD_PORT__`) to copy. | `knowledge/frontend/conventions/vite-build-time-globals.md` | 2026-07-29 |

### dev-tooling
| Topic | Description (when to load) | Path | Updated |
|-------|----------------------------|------|---------|
| DevBuildSiteCard — in-UI indicator + switcher for "am I on the Vite dev server or the built bundle" | Load when touching `client/src/components/DevBuildSiteCard.tsx`, the top of `Sidebar.tsx` (brand/title block area), the `__DASHBOARD_PORT__` build-time global, or when asked to add/modify a "which environment/mode is this" UI affordance. Also relevant background for anyone confused about whether a dashboard tab is hot-reloading or showing a stale build — this component is the shipped fix for exactly that confusion. | `knowledge/frontend/dev-tooling/dev-build-site-indicator.md` | 2026-07-29 |

### ui-decisions
| Topic | Description (when to load) | Path | Updated |
|-------|----------------------------|------|---------|
| Decision — ConcurrencyStatTile defaults to "active" (not "open") as its primary ratio | Load when working on `client/src/components/ConcurrencyStatTile.tsx`, the `agent-monitor-concurrency-primary` localStorage key, `FocusReportBody` / `FocusReportModal`, `FocusCalendarBoard`, or `FocusPage` concurrency displays — or when asked "why does the concurrency tile default to X" / asked to change the default again. Distinguishes open-session-wall-clock concurrency from active-agent-time concurrency. | `knowledge/frontend/ui-decisions/concurrency-stat-tile-default.md` | 2026-07-29 |

## product

### naming
| Topic | Description (when to load) | Path | Updated |
|-------|----------------------------|------|---------|
| Two unrelated "Focus" features share the name in this codebase — session Focus-time analytics vs. `ccam focus` plan-item declaration | Load whenever a task, bug report, or request mentions "Focus" anywhere in Claude-Code-Agent-Monitor — `FocusPage.tsx`, `FocusCalendarBoard.tsx`, `FocusReportModal`/`FocusReportBody`, AGENT-PLAN.md, or CLI usage like `ccam focus set/push/pop/done/status` — before writing code, docs, or answering a question, to make sure the right "Focus" is meant. These are two genuinely different features that happen to share a name and are easy to conflate. | `knowledge/product/naming/two-focus-concepts.md` | 2026-07-29 |

<!-- Repeat per domain/subdomain. Keep alphabetical within a subdomain. -->

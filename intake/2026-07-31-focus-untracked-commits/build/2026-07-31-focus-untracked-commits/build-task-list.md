# Build Task List — 2026-07-31-focus-untracked-commits

> Authored by `build-planner`, merging `technical-plan.md` (what to change) and
> `test-plan.md` (what to prove) into ONE ordered, dependency-correct sequence.
> The implementer follows this top to bottom, test-first, in a single sequential
> pass. Detailed enough to build without re-reading the original investigation.

**Worktree (use this, not the main checkout):**
`/Users/sara/CODE-LOCAL/SARA/efforts/2026-07-31-focus-untracked-commits/Claude-Code-Agent-Monitor`
Branch `effort/2026-07-31-focus-untracked-commits`, cut from `master` at `dfe9208`.
All file paths below are relative to this worktree's root.

**No Docker stack for this effort** (build-brief.md: not provisioned — this
effort's whole verification loop is `npm run test:server` (Node's built-in
test runner, in-process, no external DB) and `cd client && npx vitest run`
(Vitest + RTL, jsdom, no browser). No port block to reference.

## Build order (dependency-correct, red-first)
> Tests that prove a change come BEFORE the change they guard. `[test]` = author
> a failing test; `[impl]` = product code; `MANDATORY [<catalog-id>]` = durable
> cure, gates done. Catalog IDs `DERIVED-DUAL-VIEW` (§9.1) and
> `row-id-as-chronology-proxy` (§9.2) are being **established by this same
> build** (task #13) — cited here as forward references that must resolve
> before the build is declared done.

| # | Task | Type | Layer | File(s) | Done-check |
|---|------|------|-------|---------|------------|
| 1 | Author `focus-inference.test.js` Cases A & B (must-add #1) inside existing `describe("buildActivityDigest", ...)`. Case A: 3 prompts with `id` order contradicting `created_at` order. Case B (trap-defeating): 800 filler `Bash` events at `id` 1..800 (later `created_at`, transaction-wrapped) then one target `UserPromptSubmit` at `id` 801 with the *earliest* `created_at`, inserted last. Add local `addEventAt(sessionId, eventType, toolName, data, createdAtIso)` helper (column order `session_id, agent_id, event_type, tool_name, summary, data, created_at` per `server/db.js:1604-1612`). | test | server/unit | `server/__tests__/focus-inference.test.js` | `node --test server/__tests__/focus-inference.test.js --test-name-pattern "buildActivityDigest"` — run against **current unfixed** `focus-inference.js` (`ORDER BY id ASC LIMIT ?` still live at line ~123). Case A FAILS (prompts come back in id order). Case B FAILS (target event dropped by SQL `LIMIT` before any order is even relevant). Record the red output. |
| 2 | **Apply the fix.** `buildActivityDigest()`'s query: `ORDER BY id ASC LIMIT ?` → `ORDER BY created_at ASC, id ASC LIMIT ?`, with the explanatory comment from technical-plan §4.2 (cites `b3a2cc9`, explains why sort-before-`LIMIT` matters). | impl | server/lib | `server/lib/focus-inference.js` | `MANDATORY [row-id-as-chronology-proxy, PROJECT-CONTEXT.md §9.2 — added by task #13]`. Done-check: diff matches technical-plan §4.2's fixed code exactly; **must be the SQL-level `ORDER BY` change** — grep confirms no `.sort(` was added to `buildActivityDigest` as a substitute (a JS-level post-`.all()` sort passes Case A but not Case B; not an acceptable implementation of this task). |
| 3 | Re-run the same command. Confirm Cases A & B now PASS, and the 2 pre-existing `buildActivityDigest` tests are unchanged and still pass. | test | server/unit | `server/__tests__/focus-inference.test.js` | `node --test server/__tests__/focus-inference.test.js --test-name-pattern "buildActivityDigest"` — all green. |
| 4 | Author `client/src/hooks/__tests__/useHourWindowZoom.test.ts` (new file — first under this test dir), `renderHook`-based, 4 cases: (1) `windowStartMs`/`windowMs` for 4h/8h/12h/24h; (2) `windowIsFuture` true/false at exact boundary (strict `>`); (3) `customOffsetMs` survives a day-navigation round trip, `effectiveAnchorMode` reads `"custom"` on non-today; (4) **must-add #3, render-cascade regression** — several unrelated `rerender()` calls without advancing the clock → assert `windowStartMs`/`windowEndMs` bit-identical; advance fake time 1ms (no tick crossed) → still no change; advance by `ZOOM_REFRESH_MS` → window *does* re-anchor; `renderCount` equals exactly the driven render count; `vi.spyOn(console, "error")` → assert no call includes `"Maximum update depth exceeded"`. | test | client/hook-test | `client/src/hooks/__tests__/useHourWindowZoom.test.ts` | `cd client && npx vitest run src/hooks/__tests__/useHourWindowZoom.test.ts` — run against **current unfixed** `useHourWindowZoom.ts` (`forceRefresh` bump-counter still live at lines ~129-134). Case 4 FAILS (bit-identical assertion diverges once the fake clock advances even 1ms). Cases 1-3 are pure formula/value pins, expected to PASS on both old and new code — not part of the red-first proof. |
| 5 | **Apply the fix.** Replace the `forceRefresh` bump-counter with `const [nowMs, setNowMs] = useState(() => Date.now())` + a `useEffect` that, only when `isLiveZoom`, runs `setInterval(() => setNowMs(Date.now()), ZOOM_REFRESH_MS)`. Use `nowMs` (not `Date.now()`) in the live-zoom `windowStartMs`/`windowEndMs` computation (current lines ~145-147) and in `windowIsFuture` (current line ~185). Do **not** touch `FocusCalendarView.tsx`'s effect dependency array. | impl | client/hook | `client/src/hooks/useHourWindowZoom.ts` | `MANDATORY [fix-at-source obligation, build-brief.md "Durable-cure obligations" #3; render-stability / no-runaway-effect-loop — named as a 3rd catalog-candidate in `qa/supporting/risk.md` §2 item 3]`. Done-check: diff matches technical-plan §4.1's fixed code exactly; `windowIsFuture` reads `nowMs`, not a second independent `Date.now()` call; `FocusCalendarView.tsx:453-456`'s effect and its dependency array are byte-unmodified (removing `windowStartMs`/`windowEndMs` from that array is the explicitly disallowed stale-closure symptom-patch — do not do this instead). |
| 6 | Re-run `useHourWindowZoom.test.ts` — confirm case 4 PASSES in full (value stability, tick re-anchor, exact `renderCount`, no console.error match) and cases 1-3 unchanged. Then run the integration-level cross-check across all three consumer trees and manually scan stderr. | test | client/hook-test + integration | `client/src/hooks/__tests__/useHourWindowZoom.test.ts`, `client/src/components/__tests__/FocusReportModal.test.tsx`, `client/src/components/__tests__/FocusCalendarView.test.tsx`, `client/src/pages/__tests__/FocusPage.test.tsx` | `cd client && npx vitest run src/hooks/__tests__/useHourWindowZoom.test.ts` green; then `cd client && npx vitest run src/components/__tests__/FocusReportModal.test.tsx src/components/__tests__/FocusCalendarView.test.tsx src/pages/__tests__/FocusPage.test.tsx` green **and** no `"Maximum update depth exceeded"` appears anywhere in captured stderr. |
| 7 | Author the `[FocusPage extension of the standing template]` case (must-add #2) immediately after the existing `[board-mode extension]` test. Extend the file's `vi.mock("../../lib/api", ...)` with `api.focusReport`, `api.focusReportSummary`, `api.focusReportSummaryConfig`, `api.projects.list`, `api.sessions.list`; add `vi.mock("../../lib/focusStore", () => ({ useFocusMap: () => new Map() }))`. Build **one** `report = makeReport({...})` fixture and feed the **same object reference** to both `api.projects.focusReport` (for `FocusReportModal`) and `api.focusReport` (for `FocusPage`). Render both trees in the same test, scope queries with `within(modalContainer)`/`within(pageContainer)`. Assert: (a) identical `active_ms`/`idle_ms` strings at unwindowed 24h; (b) identical on-item/off-plan % computed from the fixture, not a literal; (c) after clicking `"4h"` in **both** trees, windowed active/idle totals still agree. | test | client/component-test | `client/src/components/__tests__/FocusReportModal.test.tsx` | `MANDATORY [DERIVED-DUAL-VIEW, PROJECT-CONTEXT.md §9.1 — added by task #13]`. Done-check: test file compiles and the new `it(...)` block exists using the shared fixture reference (single-source-of-truth guardrail — do **not** construct two separately-authored "equivalent" fixture objects for the two trees, even if their literal values match). Red/green proof is task #8. |
| 8 | **Prove the new test actually pins parity, not just "renders without throwing."** Temporarily introduce a one-line divergence (e.g. change `FocusPage.tsx`'s `onItemPct` rounding from `Math.round` to `Math.floor`, or swap which of `totals.active_ms`/`totals.idle_ms` a `StatTile` reads). Run the suite, confirm the new case FAILS. Revert the divergence, confirm it PASSES. Record this red/green check in the PR description — no historical bug anchors this one, it is a genuinely new assertion. | test | client/component-test | `client/src/components/__tests__/FocusReportModal.test.tsx`, `client/src/pages/FocusPage.tsx` (temporary, reverted) | `cd client && npx vitest run src/components/__tests__/FocusReportModal.test.tsx` — FAILS with the divergence in place, PASSES after revert. Both outcomes captured in writing before moving on. |
| 9 | Delete/redirect `FocusPage.test.tsx`'s current independently-hardcoded `75%`/`25%` assertion (~line 350) to point at the new cross-view parity test as the source of truth. Leave a one-line comment — don't let the two silently diverge going forward. | impl | client/page-test | `client/src/pages/__tests__/FocusPage.test.tsx` | `cd client && npx vitest run src/pages/__tests__/FocusPage.test.tsx` passes; grep confirms no independent hardcoded `75%`/`25%` assertion remains in this file. |
| 10 | Backfill (D1, should-add, no live bug). New `describe("buildSessionFocusReport - high interval volume", ...)`: bulk-insert (transaction-wrapped) 70,000 synthetic plain events inside one open Focus segment; call `buildSessionFocusReport` inside `assert.doesNotThrow`; additionally assert `active_ms <= wall_ms` and `active_ms + idle_ms === wall_ms`. Prove it's real: temporarily revert the loop-push back to `push(...intervals)`, confirm `RangeError: Maximum call stack size exceeded` and a failing test; revert to current fixed code, confirm it passes. | test | server/unit | `server/__tests__/focus-report.test.js`, `server/lib/focus-report.js` (temporary, reverted) | `node --test server/__tests__/focus-report.test.js` — fails with the old `push(...intervals)` temporarily restored, passes on current `master` code. |
| 11 | Backfill (D2, should-add, no live bug). New `client/src/components/__tests__/ConcurrencyStatTile.test.tsx`: (1) normal input renders correct primary/secondary ratio, swap button inverts + persists `CONCURRENCY_PRIMARY_KEY` to `localStorage`; (2) `activeConcurrencyRatio: null` (or `concurrencyRatio: null`, whichever is active primary at assertion time) renders `"—"` without throwing, swap button still present; (3) `label` prop override renders custom text. Prove case 1 is real: temporarily break the toggle (comment out `setPrimary`'s call, or hardcode `activeIsPrimary = true`), confirm fail, revert, confirm pass. | test | client/component-test | `client/src/components/__tests__/ConcurrencyStatTile.test.tsx`, `client/src/components/ConcurrencyStatTile.tsx` (temporary, reverted) | `cd client && npx vitest run src/components/__tests__/ConcurrencyStatTile.test.tsx` — fails with the toggle broken, passes on real code. |
| 12 | Backfill (D3, should-add, no live bug — **no red-first expected**, already-correct shipped code). New `server/__tests__/settings-export.test.js`: real `GET /api/settings/export` against the running app (`createApp`/`startServer`, hand-rolled `http` fetch, no supertest, matching this repo's per-file convention); assert HTTP 200, `Content-Type: application/json`, `Content-Disposition` filename pattern, valid-JSON-parseable body, seeded row counts per table match exactly, at least one field value round-trips, ordering matches the route's own `ORDER BY started_at DESC`/`created_at DESC`, and an empty-DB call still produces valid JSON with `[]` arrays. | test | server/unit | `server/__tests__/settings-export.test.js` | `node --test server/__tests__/settings-export.test.js` — green on first run against current, already-correct code. **Stop-and-report trigger:** if this does NOT pass on first run, that indicates a real bug in the already-shipped `60af828` streaming route — do not force it green, escalate. |
| 13 | Add `## Recurring defect-class patterns` section to `PROJECT-CONTEXT.md` (currently only has `## Repo topology`) — paste technical-plan.md §9 text **verbatim**, both entries in order (`### 9.1 DERIVED-DUAL-VIEW`, `### 9.2 row-id-as-chronology-proxy`), including the "Flagged in" citations and acceptance criteria. | impl | docs | `PROJECT-CONTEXT.md` (repo root) | `MANDATORY [establishes this project's first two defect-catalog entries — the forward references in tasks #2 and #7 above resolve only once this lands]`. Done-check: section present, text matches technical-plan §9 exactly; no file-header needed (`.md` is excluded per `.claude/rules/file-headers.md`'s applicable-extension list). **Must land before task #16 (final gate)** — order relative to tasks 10-12 doesn't matter, but it cannot be the last thing skipped. |
| 14 | Write `intake/2026-07-31-focus-untracked-commits/decisions.md` (new file). DEC-7 and DEC-8: paste technical-plan.md §8 text **verbatim**. DEC-9 (new, not in technical-plan §8's draft — per build-brief.md's "Durable-cure obligations" #5): record that the two structural guards test-plan names (a `FocusReport`-consumer registry meta-test; an `ORDER BY id`-without-`created_at` AST/grep guard) were **not built** in this pass — point-tests-only scope, per test-plan's "Durable-cure decision" section. Status **PENDING — flagged for Sara's input, not decided by the build team**. Cite: this is the 4th recorded `DERIVED-DUAL-VIEW` instance and 3rd `row-id-as-chronology-proxy` instance; a prior QA run (`2026-07-28-wip-queue-page`) already recommended formalizing the `DERIVED-DUAL-VIEW` registry meta-test after the 3rd occurrence and it was not acted on before this 4th instance shipped. | impl | docs | `intake/2026-07-31-focus-untracked-commits/decisions.md` | Done-check: file exists with DEC-7, DEC-8, and DEC-9; DEC-9 is explicitly labeled **PENDING** (not `DECIDED`), matches build-brief.md open question #2's framing, and is surfaced in the build's closing report rather than silently resolved either way. |
| 15 | **No-op / scope note.** Confirm the stale `FocusReportModal.tsx` → `FocusReportBody.tsx` file-path reference (`intake/status-report.md`'s DOC CLEANUP finding) lives in the **sibling** item's report — `intake/2026-07-26-focus-report-fidelity/build/2026-07-26-focus-report-fidelity/build-report.md` — not in any file this effort's technical-plan.md §5 change set or §11 Definition of Done names. `intake/status-report.md`'s own backlog labels this fix "Manual doc fix (not a skill invocation)" as a separate action item, distinct from this build. Take **no action** on that sibling file. | none (verification) | docs / scope-check | `intake/2026-07-26-focus-report-fidelity/build/2026-07-26-focus-report-fidelity/build-report.md` (verify only, do not edit) | Done-check: `git status` on the worktree shows no changes under `intake/2026-07-26-focus-report-fidelity/`; this out-of-scope call is stated explicitly in the build's closing report (not silently done, not silently skipped). |
| 16 | **Final gate — full suite.** Run both full suites and confirm counts grew by exactly the number of new test cases added across tasks 1, 4, 7, 10, 11, 12. | test | full-suite | (repo-wide) | `npm run test:server` → `1047+N/1047+N`. `cd client && npx vitest run` → `645+N/645+N`. Both green in full. |
| 17 | **File-header audit.** Confirm every new/edited applicable file (`.js/.ts/.tsx`) carries the mandatory header. New files: `useHourWindowZoom.test.ts`, `ConcurrencyStatTile.test.tsx`, `settings-export.test.js`. Edited files: `focus-inference.js`, `useHourWindowZoom.ts` (confirm the header's file-overview comment still accurately describes the file post-edit, not just that a header exists). | test | full-suite | (repo-wide) | `bash .claude/skills/file-headers/scripts/check-headers.sh` exits 0. |
| 18 | **Snapshot check.** Run the client suite once more, watching `screens.snapshot.test.tsx` specifically. Neither fix (§4.1, §4.2) changes rendered output — only timing/ordering — so no diff is expected. | test | full-suite | `client/src/pages/__tests__/screens.snapshot.test.tsx` | `cd client && npx vitest run` — no snapshot diff. If one appears: investigate as a fix-correctness signal per `.claude/rules/docs-markdown.md`/this repo's testing policy — do **not** blind-accept with `-u`. |

## Mandatory durable-cure tasks

- **#2 — SQL-level `ORDER BY created_at ASC, id ASC LIMIT ?` fix in `focus-inference.js`** `MANDATORY [row-id-as-chronology-proxy, PROJECT-CONTEXT.md §9.2, added by #13]` — closes the highest-severity risk this build carries (per `qa/supporting/risk.md` §5: "silent, data-loss-adjacent... the closest thing in this change-set to silent data corruption reaching a user-facing surface"). This is the 3rd recorded instance of the pattern and structurally worse than the prior two (`6e9a443`, `b3a2cc9`) because a bad `LIMIT` can **drop** the correct row entirely, not just present a correct set in the wrong order. A JS-level `.sort()` after `.all()` — the `b3a2cc9`-style fix, reapplied naively — would pass Case A (task #1) but fail Case B, and must not be accepted as satisfying this task.
- **#5 — `nowMs`-state fix in `useHourWindowZoom.ts`** `MANDATORY [fix-at-source obligation, build-brief.md "Durable-cure obligations" #3; render-stability/no-runaway-effect-loop, named a 3rd catalog-candidate in qa/supporting/risk.md §2 item 3]` — must stabilize the hook's own value identity at its one source. Must **not** be solved by removing `windowStartMs`/`windowEndMs` from `FocusCalendarView.tsx`'s effect dependency array (the explicitly disallowed stale-closure symptom-patch named in technical-plan §4.1) — that would silence the console warning while leaving the hook still producing a new value identity on every render, a latent hazard for any future consumer.
- **#7/#8/#9 — `DERIVED-DUAL-VIEW` cross-view parity test + hardcoded-assertion redirect** `MANDATORY [DERIVED-DUAL-VIEW, PROJECT-CONTEXT.md §9.1, added by #13]` — this is the literal reason this intake exists: 4th recorded instance of the pattern, zero durable regression guard on `FocusPage` until this test lands (`qa/supporting/risk.md` §3: "if the render-cascade fix lands without that cross-view test also landing, the project ships a 4th recorded instance of this exact pattern with *still* no regression guard"). Must use a single shared fixture **object reference** fed to both mocked endpoints — never two separately-authored "equivalent" fixtures, per the plans' own single-source-of-truth guardrail.
- **#13 — `PROJECT-CONTEXT.md` catalogue additions** `MANDATORY [establishes DERIVED-DUAL-VIEW §9.1 and row-id-as-chronology-proxy §9.2 as this project's first two formally catalogued defect-class entries]` — without this landing, the `MANDATORY [...]` citations on tasks #2 and #7 above point at catalog IDs that don't yet exist anywhere in the repo. Must land in this same build, not deferred to a follow-up.
- **Deferred, NOT built this pass — flagged as an open decision, not a silently-encoded shortcut:** the two *structural* guards a stricter reading of "durable cure" would call for — a `FocusReport`-consumer registry meta-test (fails automatically when a new consumer is added without a parity-chain entry) and an `ORDER BY id`-without-`created_at` AST/grep guard over the `events` table — are explicitly **out of scope** for this pass per `test-plan.md`'s "Durable-cure decision" section (point-tests-only). Given this is the 4th/3rd occurrence of each pattern respectively, and a prior QA run already recommended formalizing the `DERIVED-DUAL-VIEW` guard after the 3rd occurrence without action, this deferral is **not** a build-team call to finalize — task #14 records it in `decisions.md` as **DEC-9, PENDING**, surfaced to Sara rather than silently accepted or silently built.

## Sequencing notes

- **Do not parallelize — one sequential implementer.** Hard orderings: #1 before #2 before #3 (red before fix before green, chronology bug); #4 before #5 before #6 (same, render cascade); #7 before #8 before #9 (write parity test, prove it's real, then redirect the old hardcoded assertion). #13 (`PROJECT-CONTEXT.md`) can be written any time after the two fixes conceptually exist, but **must** land before #16's final gate, since #2 and #7's `MANDATORY` citations forward-reference it. #10, #11, #12 (backfill, workstream D) have no fix to sequence against and can slot in anywhere after #9 without breaking any dependency — placed here in test-plan's own priority order.
- **Needs services up vs. pure unit-level:** nothing in this build needs a Docker stack or a manually-started dev server. Task #12 (`settings-export.test.js`) starts the Express app **in-process** via `createApp`/`startServer` on an ephemeral port inside the test file itself — this is unit/functional-level, not a call to this effort's (non-existent) Docker stack. Every other task is pure unit/component-level (`node --test`, Vitest+jsdom).
- **Stop-and-report triggers (do not improvise past these):**
  - Task #1 or #4: if the described red state does **not** reproduce (e.g. Case B unexpectedly passes, or case 4 unexpectedly passes) — stop. The root-cause understanding may be stale relative to current `master`; do not apply the fix blind.
  - Task #12: if `settings-export.test.js` does **not** pass on first run — this is already-shipped, already-correct code by both plans' own read; a failure here is a **real bug**, not an expected red state. Stop and report, don't force green.
  - Either fix (#2 or #5) turning out mid-implementation to need real redesign (not a contained one-file change as both plans assert) — per technical-plan §2, stop and split it into a separate intake folder rather than improvising a broader change here.
  - Task #18: any snapshot diff — investigate before regenerating with `-u`; do not blind-accept.
  - Anything that would require deciding DEC-9 one way or the other (building either structural guard, or explicitly declaring the deferral permanent) — that decision belongs to Sara, not the implementer. Record PENDING and move on.
